import React,{useMemo,useState}from'react';import{createRoot}from'react-dom/client';import{LayoutDashboard,Users,FileWarning,ShieldCheck,HeartPulse,FileSpreadsheet,Settings,Search,Plus,Upload,ChevronLeft,Bell,BriefcaseBusiness}from'lucide-react';import'./styles.css';

type Emp={id:string;name:string;job:string;dept:string;nationality:string;identity:string;iqama:string;passport:string;workPermit:string;insurance:string;status:string;salary:number};
const seed:Emp[]=[{id:'1001',name:'محمد أحمد علي',job:'مهندس مكتب فني',dept:'الإدارة الهندسية',nationality:'مصري',identity:'2*********',iqama:'2027-01-18',passport:'2028-05-20',workPermit:'2026-12-10',insurance:'2027-03-01',status:'على رأس العمل',salary:8500},{id:'1002',name:'عبدالله محمد',job:'مهندس مدني',dept:'المشروعات',nationality:'سعودي',identity:'1*********',iqama:'2027-09-14',passport:'2029-01-11',workPermit:'2027-09-14',insurance:'2027-06-20',status:'على رأس العمل',salary:12000},{id:'1003',name:'أحمد حسن',job:'محاسب',dept:'المالية',nationality:'مصري',identity:'2*********',iqama:'2026-10-02',passport:'2027-08-09',workPermit:'2026-10-02',insurance:'2026-09-28',status:'على رأس العمل',salary:7000}];
const nav=[['dashboard','لوحة التحكم',LayoutDashboard],['employees','الموظفون',Users],['documents','المستندات والتنبيهات',FileWarning],['insurance','التأمين الطبي',HeartPulse],['gosi','التأمينات الاجتماعية',ShieldCheck],['reports','التقارير',FileSpreadsheet]] as const;
function days(d:string){return Math.ceil((new Date(d).getTime()-Date.now())/86400000)}
function App(){const[page,setPage]=useState('dashboard');const[emps,setEmps]=useState(seed);const[selected,setSelected]=useState<Emp|null>(null);const[query,setQuery]=useState('');const filtered=useMemo(()=>emps.filter(e=>(e.name+e.id+e.job+e.identity).includes(query)),[emps,query]);const alerts=emps.flatMap(e=>[['الإقامة',e.iqama],['رخصة العمل',e.workPermit],['التأمين الطبي',e.insurance]].map(([type,date])=>({e,type,date,n:days(date)}))).filter(x=>x.n<=60).sort((a,b)=>a.n-b.n);
 const title=nav.find(n=>n[0]===page)?.[1]||'لوحة التحكم';return <div className="app"><aside><div className="brand"><div className="logo">HR</div><div><b>HR Saudi</b><small>إدارة الموارد البشرية</small></div></div><nav>{nav.map(([id,label,Icon])=><button className={page===id?'active':''} onClick={()=>{setPage(id);setSelected(null)}}><Icon size={19}/><span>{label}</span></button>)}</nav><div className="side-bottom"><button><Settings size={18}/> الإعدادات</button><div className="user"><div>أ</div><span>مسؤول النظام<small>Administrator</small></span></div></div></aside><main><header><div><h1>{title}</h1><p>نظام إدارة الموارد البشرية — المملكة العربية السعودية</p></div><div className="header-actions"><button className="icon"><Bell size={20}/><i>{alerts.length}</i></button><div className="avatar">أ</div></div></header>{page==='dashboard'&&<Dashboard emps={emps} alerts={alerts} setPage={setPage} setSelected={setSelected}/>} {page==='employees'&&<Employees emps={filtered} query={query} setQuery={setQuery} setSelected={setSelected} setEmps={setEmps}/>} {page==='documents'&&<Documents alerts={alerts} setSelected={setSelected}/>} {page==='insurance'&&<Insurance emps={emps}/>} {page==='gosi'&&<Gosi emps={emps}/>} {page==='reports'&&<Reports emps={emps}/>} {selected&&<EmployeeDrawer e={selected} close={()=>setSelected(null)}/>}</main></div>}
function Dashboard({emps,alerts,setPage,setSelected}:{emps:Emp[];alerts:any[];setPage:(x:string)=>void;setSelected:(x:Emp)=>void}){const cards=[['إجمالي الموظفين',emps.length,Users,'blue'],['السعوديون',emps.filter(e=>e.nationality==='سعودي').length,BriefcaseBusiness,'green'],['تنبيهات خلال 60 يوم',alerts.length,FileWarning,'orange'],['التأمينات النشطة',emps.length,ShieldCheck,'purple']];return <section><div className="cards">{cards.map(([t,v,I,c])=><div className="card"><div className={'cardicon '+c}><I size={22}/></div><div><span>{t}</span><strong>{v}</strong></div></div>)}</div><div className="grid"><div className="panel"><div className="panel-head"><div><h2>التنبيهات القادمة</h2><p>المستندات التي تحتاج إلى إجراء</p></div><button className="link" onClick={()=>setPage('documents')}>عرض الكل <ChevronLeft size={16}/></button></div>{alerts.slice(0,5).map(a=><div className="alert"><div className="alert-icon"><FileWarning size={18}/></div><div className="alert-info"><b>{a.type} — {a.e.name}</b><span>{a.date}</span></div><strong className={a.n<0?'danger':''}>{a.n<0?'منتهي':`متبقي ${a.n} يوم`}</strong></div>)}</div><div className="panel"><div className="panel-head"><div><h2>الموظفون</h2><p>آخر الموظفين المسجلين</p></div><button className="link" onClick={()=>setPage('employees')}>عرض الموظفين</button></div>{emps.slice(0,5).map(e=><div className="person" onClick={()=>setSelected(e)}><div className="avatar sm">{e.name[0]}</div><div><b>{e.name}</b><span>{e.job}</span></div><small>{e.dept}</small></div>)}</div></div></section>}
function Employees({emps,query,setQuery,setSelected,setEmps}:{emps:Emp[];query:string;setQuery:(x:string)=>void;setSelected:(x:Emp)=>void;setEmps:(x:Emp[])=>void}){return <section><div className="toolbar"><div className="search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="بحث بالاسم أو رقم الهوية أو الوظيفة..."/></div><button className="secondary"><Upload size={17}/> استيراد Excel</button><button className="primary" onClick={()=>alert('سيتم فتح نموذج إضافة موظف في الإصدار التالي')}><Plus size={18}/> إضافة موظف</button></div><div className="panel table-panel"><div className="table-title"><div><h2>سجل الموظفين</h2><p>{emps.length} موظف</p></div></div><table><thead><tr><th>الموظف</th><th>الهوية / الإقامة</th><th>المسمى الوظيفي</th><th>الجنسية</th><th>الإقامة</th><th>الحالة</th><th></th></tr></thead><tbody>{emps.map(e=><tr onClick={()=>setSelected(e)}><td><div className="person"><div className="avatar sm">{e.name[0]}</div><div><b>{e.name}</b><span>#{e.id}</span></div></div></td><td>{e.identity}</td><td>{e.job}</td><td>{e.nationality}</td><td><span className={days(e.iqama)<=60?'expiry':''}>{e.iqama}</span></td><td><em>{e.status}</em></td><td><ChevronLeft size={17}/></td></tr>)}</tbody></table></div></section>}
function Documents({alerts,setSelected}:{alerts:any[];setSelected:(x:Emp)=>void}){return <section><div className="panel table-panel"><div className="table-title"><div><h2>مركز التنبيهات والمستندات</h2><p>تنبيه تلقائي قبل الاستحقاق بـ 60 يومًا مع قابلية التعديل من الإعدادات</p></div></div><table><thead><tr><th>الموظف</th><th>المستند</th><th>تاريخ الانتهاء</th><th>الحالة</th></tr></thead><tbody>{alerts.map(a=><tr><td><b>{a.e.name}</b></td><td>{a.type}</td><td>{a.date}</td><td><span className={a.n<0?'tag red':'tag orange'}>{a.n<0?'منتهي':`خلال ${a.n} يوم`}</span></td></tr>)}</tbody></table></div></section>}
function Insurance({emps}:{emps:Emp[]}){return <section><div className="cards"><div className="card"><div className="cardicon green"><HeartPulse/></div><div><span>المؤمن عليهم</span><strong>{emps.length}</strong></div></div><div className="card"><div className="cardicon orange"><FileWarning/></div><div><span>وثائق تحتاج متابعة</span><strong>{emps.filter(e=>days(e.insurance)<=60).length}</strong></div></div></div><div className="panel table-panel"><div className="table-title"><h2>سجل التأمين الطبي</h2></div><table><thead><tr><th>الموظف</th><th>شركة التأمين</th><th>رقم الوثيقة</th><th>تاريخ الانتهاء</th></tr></thead><tbody>{emps.map(e=><tr><td>{e.name}</td><td>شركة التأمين الطبي</td><td>POL-{e.id}-2026</td><td>{e.insurance}</td></tr>)}</tbody></table></div></section>}
function Gosi({emps}:{emps:Emp[]}){return <section><div className="cards"><div className="card"><div className="cardicon purple"><ShieldCheck/></div><div><span>المشتركون</span><strong>{emps.length}</strong></div></div><div className="card"><div className="cardicon blue"><FileSpreadsheet/></div><div><span>إجمالي الأجور الشهرية</span><strong>{emps.reduce((a,e)=>a+e.salary,0).toLocaleString()} ر.س</strong></div></div></div><div className="notice"><ShieldCheck/><div><b>إعدادات الاشتراكات</b><p>النسب لا يتم تثبيتها داخل الكود، ويمكن تحديثها من إعدادات النظام وفق آخر المتطلبات الرسمية.</p></div></div></section>}
function Reports({emps}:{emps:Emp[]}){return <section><div className="report-grid">{[['تقرير الموظفين','بيانات الموظفين الأساسية'],['تقرير الإقامات','الصلاحية والتنبيهات'],['تقرير رخص العمل','حالة وتواريخ الانتهاء'],['تقرير التأمين الطبي','البوليصات والانتهاء'],['تقرير التأمينات','الاشتراكات والأجور'],['تقرير التكاليف','تكلفة الموظفين الشهرية']].map(x=><div className="report"><FileSpreadsheet/><b>{x[0]}</b><span>{x[1]}</span><button>فتح التقرير</button></div>)}</div></section>}
function EmployeeDrawer({e,close}:{e:Emp;close:()=>void}){return <div className="overlay" onClick={close}><div className="drawer" onClick={x=>x.stopPropagation()}><div className="drawer-head"><button className="close" onClick={close}>×</button><div className="avatar lg">{e.name[0]}</div><div><h2>{e.name}</h2><p>{e.job} · {e.dept}</p></div></div><div className="drawer-section"><h3>البيانات الأساسية</h3><div className="facts"><label>الرقم الوظيفي<strong>{e.id}</strong></label><label>الجنسية<strong>{e.nationality}</strong></label><label>الهوية / الإقامة<strong>{e.identity}</strong></label><label>الراتب الأساسي<strong>{e.salary.toLocaleString()} ر.س</strong></label></div></div><div className="drawer-section"><h3>المستندات</h3>{[['الإقامة',e.iqama],['جواز السفر',e.passport],['رخصة العمل',e.workPermit],['التأمين الطبي',e.insurance]].map(x=><div className="doc"><span>{x[0]}</span><b>{x[1]}</b></div>)}</div></div></div>}
createRoot(document.getElementById('root')!).render(<App/>);
